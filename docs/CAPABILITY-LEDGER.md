# Capability and authority ledger

C12 adds CLI-only `/action/*` context, queue, show/list/history and cancellation. The daemon derives task/context/manifest/attempt/authority/postcondition records and journals dispatch before browser delivery. Browser result ingress retains exact issued action/attempt identity and cannot supply a matched postcondition. Unpairing, changed context, stale connection and finished/refused attempts block cached redelivery. Scoped/MCP credentials gain no action access. Legacy browser history remains unscoped/API-reported and unverified; legacy controls cannot operate on shared task-owned tabs. Schema 16 preserves earlier credentials, events and receipts. See [shared actions](ACTIONS-SETUP.md).

W04 adds local CLI-only workspace List/Diff/preview/validate. These routes observe only the selected task, perform no durable mutation and dispatch no application actions. A 30-second daemon-lifetime seal authenticates a preview artifact, not execution authority. Validation independently reobserves and recomputes it; task scope, ownership and retained-point inputs are rechecked. TUI point capture uses the existing explicit snapshot command and retained request journal. Schema and scoped/MCP/browser grants remain unchanged. See [workspace preview](WORKSPACE-PREVIEW.md).

W03 adds CLI-only `/workspace/snapshot/*` capture/policy/pin/unpin/prune/status/list/show. Automatic capture runs only under a current explicitly accepted task/manifest/source policy. It stores owned surface observations, never dispatches application actions, and cannot complete tasks. All mutations revalidate current heads and references; source I/O is outside the task writer. Payload retention protects current heads and active pins and records historical unavailability. Schema 15 preserves earlier grants; scoped/MCP/browser clients acquire no snapshot access.

W02 adds local CLI-only `/workspace/viewport/*` probe, source selection, inventory, explicit bind/unbind and task views. Selection authorizes read-only background observation of one pinned compositor source. No scoped/MCP/browser credential gains desktop reads or actions. Window ownership requires exact source/stable identity and a current task/surface manifest; title/class/PID similarity grants nothing. Browser surfaces require the later C13 handshake. Schema 14 preserves previous authority; replay never opens IPC sockets.

P04 adds CLI-only dependency add/remove/history and read-only summaries. Existing scoped clients can read summaries and currently visible prerequisite relations; invisible prerequisites collapse to an opaque placeholder. Scope, expiry and cursor identity are checked on every request. No file observation, completion or dispatch authority is added. Schema 13 preserves earlier credentials and events.

P03 adds local CLI-only preservation preview/request/observe/show/list/export. Browser and scoped MCP/client credentials acquire no access. Stored observations and operator reports confer no completion or dispatch authority. Remote checks are explicit, read-only, bounded and pinned to the plan destination. No private clone is automatically bound or modified. Schema 12 retains all earlier event versions.

TUI replacement, 2026-09-08 Pacific: the browser frontend and all UI bootstrap/session
routes are retired. The Go TUI uses the existing local CLI bearer and explicit
review/command APIs. Its optional task argument is a display filter, not a grant.
It retains exact mutation requests without credentials and retries only on user
input. No machine-client authority expands. Historical UI-v2 records retain
strict replay support under schema 11. Earlier GUI entries below are historical.


2026-09-05, daemon build 0.7.0. Scoped reads and explicit checkpoint writes remain exposed through MCP; evaluator configuration and execution are CLI-only. GUI sessions add scoped inspection and explicit completion review.

R0/T01 update, 2026-09-08: readable resume and checkpoint draft/submit use the existing local CLI authority. `/continuity/resume` is a read-only CLI route; its recorded review counts are not fresh evidence evaluation. Draft creation writes only the selected local file; submission uses the existing checkpoint command and preconditions. Scoped client, MCP, browser and GUI permissions are unchanged.

W01 update, 2026-09-08: `/workspace/*` is CLI-only. Explicit manifest acceptance and session bind/unbind confer no desktop or filesystem authority; host/environment/paths and generic runtime IDs are unverified declarations. Read-only views expose task/manifest staleness without updating records. Existing browser/scoped/MCP/GUI permissions are unchanged; both browser and issued read-credential route negatives cover the new endpoints.

T02 update, 2026-09-08: `/workspace/herdr/*` remains CLI-only. The local adapter derives identity from an explicitly selected socket/pane; callers cannot submit observed provenance. Publication rechecks the current binding while holding the writer and sends expiring display metadata only. It cannot send keys/text, launch/close panes, publish agent runtime state, or complete tasks. Optional workspace summaries explicitly name one pane. Cached receipts and replay do not re-emit metadata; interrupted SQLite commits may reapply idempotent display values, not execution actions.

T03 update, 2026-09-08 Pacific: the local Neovim plugin invokes existing CLI operations through argv arrays. Selection is explicit and drafts retain their task/request/preconditions. It opens only explicitly selected bound local artifacts and launches the existing root-scoped GUI for human completion review. The plugin adds no credential class, grant permission, daemon route or schema change, and never ratifies completion. The explicit review command copies a single-use, five-minute GUI bootstrap code to the system clipboard and opens the credential-free URL. The code is never echoed into editor messages or buffers, including through UI-message plugins. CLI endpoint credentials are read by the CLI, not loaded into editor buffers.

P02 GUI update, 2026-09-08 Pacific: `ui ROOT --progress-review` explicitly enables planning review and expanded proposal/artifact/Git inspection for a new GUI session. Ordinary GUI sessions, browser credentials and scoped MCP/read/write grants retain their previous authority. `/ui/progress` and `/ui/progress-review` require the new session flag, frozen root/resource scope and current session liveness; mutations also require exact Origin/CSRF. The writer checks live authorization before receipts and commit, and independently revalidates proposal and artifact identity. Review v2 records actor `ui:SESSION_ID` and non-secret scope/lifetime; its derived decision is v2 with matching provenance. The CLI v1 event shape still refuses any authority field, including null. Schema 11 preserves old events/receipts and refuses older binaries. Replay neither reauthorizes a session nor observes files. Neovim's new inspection/handoff commands use the existing CLI; the handoff explicitly opts into this capability without accepting any proposal.

P02 update, 2026-09-08 Pacific: all `/progress/*` routes require the existing CLI bearer. Proposals freeze task/ancestor/contract/decision identity and daemon-derived artifact observations. Reviewed/accepted outcomes require the exact proposal digest and review head plus fresh artifact checks; rejection may close stale proposals. No progress event changes task/step completion or creates evaluator evidence. Acceptance projects an accepted decision through the existing mandatory-context/evidence invalidation rules. Scoped context exposes proposal text/status only, with no expanded artifact/Git observation authority; prior read/checkpoint grants cannot submit progress operations. Payload v1 is a new event family under schema 10; old payloads remain unchanged. Exact retries and replay do not observe files or repeat review. See [progress setup](PROGRESS-SETUP.md).

P01 update, 2026-09-08 Pacific: all `/artifact/*` routes require the existing CLI bearer. Capture derives bytes/permissions, machine identity and optional Git metadata from the selected in-scope resource; submitted observations/authority are refused. Artifact checkpoints use request v2/persisted v3 with exact current version pins. Legacy checkpoint-write grants cannot submit this new format. Scoped client/MCP/GUI reads may retain opaque checkpoint references but receive `artifact_checks_require_cli` and no expanded artifact/Git checks; previous resource authority is unchanged. CLI resume performs bounded fresh checks without writes. Events, retries and replay do not retain or restore file contents, execute Git helpers, or approve completion. Browser/issued-client route negatives and scoped checkpoint tests cover these boundaries.

| Surface / identity | Implemented authority | Evidence / limit |
|---|---|---|
| Local CLI bearer | All existing CLI reads and mutations, accepted continuity records, resource registration and database backup | Authenticated loopback, exact Host, no Origin; daemon derives `actor=cli`. Caller-supplied actor is rejected. This is a trusted local credential, not cryptographic proof a human typed the command. |
| Browser-ingress bearer | Paired browser protocol ingress only | Separate credential. Cannot read task/context state, accept contracts, write checkpoints or request backups. Existing route tests plus continuity route negatives pass. |
| Browser profile | Inventory/transport identity after explicit pairing | Profile IDs are not daemon principals and cannot grant task or filesystem access. Browser content cannot authorize a command. |
| Scheduler | Existing timer/proposal transitions only | Cannot emit CLI-only continuity events. Replay never performs external effects. |
| Evidence evaluator | Finish a previously committed CLI-authorized evaluation and invalidate its evidence | Daemon-derived provenance; cannot author contracts or ratify completion. Direct argv tests have bounded execution/output and declared input coverage; they are not sandboxed. Browser/scoped credentials cannot reach evaluator routes. |
| Scoped reader | Task/subtree record and history reads; context only with all mandatory lineage/resource permissions | Separate hashed credential verifier, expiry/revocation, bounded pages, cursor-scope checks and restart rediscovery. No mutation authority. See [SCOPED-ACCESS.md](SCOPED-ACCESS.md). |
| MCP client | Task/context/history tools; checkpoint tool requires a write grant | Official SDK stdio adapter; authenticated grant provenance, authorization before dedupe/commit, structured errors and restart tests. No contract, decision, resource or completion authority. |
| Terminal UI | Existing local CLI reads and explicit mutations | Optional task/subtree display filter; same daemon revalidation and exact receipts. No browser session, cookie or sign-in code. See [TUI-SETUP.md](TUI-SETUP.md). |
| Execution host | Unknown / unavailable | No verified launch, attach, reconcile, status, cancellation or wake adapter; no automatic run resumes. |
| Braid | Not integrated | No Heimdall index publication or retrieval path. Separate repository/toolchain convenience conveys no authority. |

Resource binding authorizes observation through the trusted CLI boundary. A scoped reader additionally needs that binding ID in its grant for live context observations. Neither permits reading file contents through the client API, executing work or locking a workspace. Checkpoint text and `ready` status never imply permission to execute or accept completion.

## Required S2 policy

The current credential maps to one immutable read grant; identity is daemon-authenticated, not supplied attribution text. Scope, binding IDs, expiry and revocation are checked on each read. Revocation is serialized with authorization and response construction. Grant v1 permits only reads. Grant v2 can add checkpoint writes; authorization is checked before dedupe and again before commit, including retries. Broader mutation policy remains deferred; replayed history is not a renewed grant. A machine client cannot acquire authority by declaring `actor=cli`, choosing a known task ID, retrieving text, or submitting a contract as accepted.

Start with bounded reads and explicit checkpoint/progress grants. Reserve accepting contracts/decisions, changing bindings, ratifying completion and granting authority for the user-controlled surface unless a later policy explicitly delegates that operation. Distinguish inherited visibility from mutation authority; applying a grant to one child must not expose siblings or let it replace ancestor contracts.

MCP and daemon tests now cover wrong-scope reads/writes, sibling/ancestor access, forged authority, expiry, revocation, credential separation, duplicate commands after revocation, pagination/cursor scope and reconnect. The daemon remains the sole writer. MCP capabilities are advertised after adapter/handshake acceptance; per-host installation remains separate. Do not hand the unrestricted CLI token to an MCP client as a shortcut.
